#Write and test a query to list the name and salary of all the instructors
Select INSTRUCTOR_NAME, Salary*12 
From Instructor
Where Salary * 12 > 60000;

#Modify the first query to show the yearly salary instead. 
Select c.course_id, o.start_date
From Course c
INNER JOIN OFFERING o
ON  c.course_ID=o.course_ID

#Modify the query in the previous step to include only instructors with a yearly salary over $60,000. Make sure the list is sorted in descending salary order.
Select s.student_id, s.student_fname, s.student_lname, c.company_name
From Student s
INNER JOIN Company c
ON s.company_id = c.company_id

#Write and test a query that lists the course ID and start date of all the offerings. Order the list by ascending course ID and descending start date.
Select s.student_id, s.student_fname, s.student_lname, c.company_name
From Student s
INNER JOIN Company c
ON s.company_id = c.company_id
Where s.gender='F';

